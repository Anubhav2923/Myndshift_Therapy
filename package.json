{
  "name": "cls",
  "version": "1.0.0",
  "description": "A comprehensive MERN stack application for daily mental health check-ins with Clerk authentication.",
  "main": "index.js",
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "build": "npm run build --prefix client",
    "start": "npm run build && npm run start --prefix server"
  },
  "author": "",
  "license": "ISC"
}
